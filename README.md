
  # Smart Expenses Tracker

  This is a code bundle for Smart Expenses Tracker 
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  